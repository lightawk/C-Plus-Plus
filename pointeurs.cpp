// Utilisation d'un pointeur (=on passe en parametres l'adresse d'un entier)
void test(int* a) {
    *a = 50;
}

int main() {
    // Modification de "a"
    //test(&a);
    //test(p_int);

    return 0;
}